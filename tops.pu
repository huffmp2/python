prompt= "\nWhat would you like on your pizza? Enter one topping at a time."
    prompt += "\nEnter 'quit' when you are finished.) "
while True:
    top= input(prompt)
if top == 'quit':
    break
else:
    print ("Mmmm...." + top.title() + "! What else would you like on your pizza?")